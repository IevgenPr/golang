package main

import (
	"fmt"
	"testing"
)

func TestBitset(t *testing.T) {

	// got := big.NewInt(1034)
	fmt.Println("num", 11)
	// want := []byte{10, 20, 30}
	// // got := BigIntToBytes(num)
	// // fmt.Println("num", got)
	// // fmt.Printf("%.8b", got[0])
	// if reflect.DeepEqual(got, want) {
	// 	t.Errorf("\n%#v \n%#v", 1, 2)
	// }

}
